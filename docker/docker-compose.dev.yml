services:
  app:
    build:
      context: ..
      dockerfile: docker/dockerfile.dev

    image: loan-approval-prediction:dev

    working_dir: /app

    volumes:
      - ..:/app

    command: sleep infinity